gitgem - like gem this, but manage gems with git + bundler
sdoc - it's awesome
module include precedence

Songs for my funeral

1) Carry Me - Malcolm Middleton
2) Mykonos - Fleet Foxes
3) The Light 3000


bash < <( curl http://rvm.beginrescueend.com/releases/rvm-install-head )

brew install readline

rvm install 1.8.7 -C --with-readline-dir=`brew --prefix`
rvm install ree -C --with-readline-dir=`brew --prefix`
rvm install 1.9.2 -C --with-readline-dir=`brew --prefix`
rvm install rbx -C --with-readline-dir=`brew --prefix`

